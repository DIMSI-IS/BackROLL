version: "3.9"

services:

  first_container:
    build: tester
    volumes:
      - ./tester/src:/app/src
    environment:
      - API_ENDPOINT_URL = Riri
      - OPENID_ISSUER = Fifi
      - OPENID_REALM = Loulou
      - OPENID_CLIENTID = Picsou
